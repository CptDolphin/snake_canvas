{
  "name": "pure_css_hamburger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx pm2 --watch src/backend/server.js --attach --max-restarts=1 start src/backend/server.js",
    "dev": "rollup -c -w --sourcemap",
    "delete": "npx pm2 delete all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pm2": "^4.1.2",
    "rollup": "^1.26.3",
    "rollup-watch": "^4.3.1"
  }
}
